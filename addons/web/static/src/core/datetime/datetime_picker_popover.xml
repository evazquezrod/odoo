<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">
    <t t-name="web.DateTimePickerPopover">
        <DateTimePicker t-props="props.pickerProps">
            <t t-set-slot="buttons">
                <div class="o_datetime_buttons d-flex gap-2">
                    <button
                        class="btn btn-sm btn-secondary"
                        tabindex="-1"
                        t-on-click="() => props.pickerProps.range || props.pickerProps.focusedDateIndex ? props.pickerProps.onSelect([false, false]) : props.pickerProps.onSelect(false)"
                    >
                        <i class="fa fa-eraser" />
                    </button>
                    <button
                        class="btn btn-sm btn-primary"
                        tabindex="-1"
                        t-on-click="props.close"
                    >
                        <span>Apply</span>
                    </button>
                </div>
            </t>
            <t t-set-slot="addDateButton" t-slot-scope="optionScope">
                <t t-set="indexDateValue" t-value="!optionScope.timeValues[0] ? 0 : 1" />
                <button
                    class="o_add_date o_add_start_date btn btn-secondary btn-sm ms-auto"
                    t-on-click="() => !props.range ? optionScope.addDate(indexDateValue) : optionScope.removeDate()"
                    t-att-class="{ 'active': props.range }"
                >
                    <i class="fa fa-calendar-plus-o" />
                </button>
            </t>
        </DateTimePicker>
    </t>
</templates>
